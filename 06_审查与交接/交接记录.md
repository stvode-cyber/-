# AI 助理系统 - 项目交接工作点记录

> 文档作用：持续同步项目开发阶段的工作节点、完成功能、已知问题与部署信息，便于后续接手人员快速进入。
> 维护规则：每次会话有重要变更时追加/更新对应章节，按时间倒序追加节点记录。

---

## 一、项目基本信息

| 项 | 内容 |
| :--- | :--- |
| 项目名称 | AI 助理系统（编号 9527：AI 全能个人助理） |
| 版本号 | MVP v1.0.0 |
| 设计文档 | `AI助理系统设计文档 V2.3 最终交付版（完整归档）.md` |
| 技术栈（前端） | React 18 + TypeScript + Vite + TailwindCSS + Zustand + axios + lucide-react |
| 技术栈（后端） | Node.js + Express + TypeScript + Prisma + SQLite + zod + jsonwebtoken |
| 部署平台 | Linux 服务器 + Nginx + PM2（Web/PWA）+ Windows 桌面端（Electron） |
| 仓库位置 | `c:\Users\Administrator\Desktop\助理项目\助理项目\APP-AIE`（原 `c:\Users\Administrator\Desktop\APP-AIE`，已迁移） |
| Node 二进制 | `C:\Users\Administrator\.trae-cn\node-runtime\node-v24.14.0-win-x64\node.exe` |

---

## 二、当前完成进度

**总体进度：≈ 99%**（核心 MVP + Phase 2 全量 + AgentCore 智能体 P0-P4 全量交付 + QQ 式社交层 + 资料库自动化聊天 + 安卓端公网接入）

- 2026-09-10 新增：管理后台独立成云端网页 ✅（管理入口不再与 APP 在一起）
  - 代码：主 APP 移除 admin 路由与两处登录页入口链接（`src/App.tsx` / `src/pages/LoginPage.tsx`）；新增独立入口 `frontend/admin.html` + `src/admin-main.tsx` + `admin-vite.config.ts`（独立构建，`publicDir:false` 置于顶层避免混入主 APP public 资源）
  - 后端：`backend/src/index.ts` 托管 `/admin`（`express.static` + SPA fallback，保留 `../public/admin` 默认目录，可用 `ADMIN_WEB_DIR` 覆盖）；不吞 `/api/v1/*`
  - 访问地址：`https://lujax.fun:8444/admin/`（nginx `lujax-fun.conf` 8444 → 后端 3001）
  - 部署：上传 `backend/dist/index.js` 与 `frontend/admin-dist/*`（admin.html + assets/ 4 文件）至 `/root/backend/dist` 与 `/root/backend/public/admin`，`pm2 restart aie-backend`；已验证 `/admin/`、深链 `/admin/users`、静态资源均 200，API 未被遮蔽
  - 桌面端已重打包完成 ✅（2026-09-10 10:25，`build-desktop.cjs` → `electron/release-v16/`，版本沿用 1.0.0）：`绿角犀-Setup-1.0.0.exe`（152MB）与 `绿角犀-Portable-1.0.0.exe`（151.5MB）；已验证打包内前端无 `AdminLayout`、无 `/admin` 路由（登录页仅剩帮助文字"联系本机管理员重置密码"，非入口链接）
  - 安卓 APK 已重打包完成 ✅（2026-09-10 12:08，移动端 `vite --mode mobile` + cap sync + gradle assembleRelease，versionName 1.0.1 / versionCode 2，正式签名 CN=LuJiaoXi）：`backend\data\绿角犀-安卓端-v1.0.1-正式签名.apk`（12.6MB，已同步覆盖 `app-release.apk`）
    - 安卓构建环境于本机现场补齐：JDK `C:\aie-toolchain\jdk-21.0.12.1+1`，SDK `C:\Users\Administrator\.android-sdk`（platform-35 + build-tools;35.0.0 + platform-tools，经腾讯镜像下载）；`local.properties` 已指向 SDK
    - 网络适配：`maven.google.com` 被墙，根 `build.gradle` 的 `google()` 已改为阿里云镜像 `maven.aliyun.com/repository/google`（构建依赖可正常下拉）；Gradle 发行版沿用本地缓存 `~/.gradle/wrapper/dists`（补 `.zip.ok` 标记）
- 2026-09-03~04 新增：AI 老朋友人设落地（agent-persona.txt + AGENT_PERSONA_PROMPT 环境变量注入，打开必激活）✅；自动化聊天统一"资料库+LLM"生成（buildPersonalizedGreeting 双调度器复用，vault 防幻觉）✅；安卓端 APK 切公网地址 47.116.59.141:3001 重新发布 ✅；公网后端部署（PM2 aie-backend + HOST=0.0.0.0 + 安全组放行 3001）✅；远程文件服务开关打开 + 手机全链路验证 ✅；开机自启（后端 start-backend.vbs + frp frpc-tunnel.vbs，GBK 编码修复）✅
- 2026-09-04 追加：安卓正式版加固 ✅（API 动态地址 ServerSettingsPage / GET 超时自动重试 / release 正式签名 keystore，脱敏后发布新 APK；HTTPS 客户端适配已做：设置页认领 https+自动补协议+安全标识，完整服务器侧待域名证书）→ APK：桌面 `绿角犀-安卓端-v1.0.1-正式签名.apk`（versionCode 2）；密钥 `frontend/android/keys/`（已 gitignore，密码勿传）
- 2026-09-04 HTTPS 服务器侧（进行中，卡凭据）：域名 `lujax.fun`（Cloudflare 解析，未 ICP 备案 → 走 8443 端口避备案）；A 记录现指向 8.149.245.252，需改指后端 47.116.59.141；方案=Let's Encrypt（Cloudflare DNS 验证）证书 + Nginx 反代 3001 → `https://lujax.fun:8443`，App 地址改之。**待用户提供**：①Cloudflare API Token（Zone:DNS:Edit）②服务器 SSH 账号密码。未拿到前此环节停滞
- 2026-09-01 新增：Obsidian 服务端记忆库（vault_notes 自动收集/检索注入/VaultPage 管理页）✅；浮窗入口统一（待办钉桌面虚线按钮 + 倒计时两页 monitor 图标）✅；便签浮窗 UI 升级（毛玻璃圆钮 + 边缘缩放 + 大小记忆）✅；倒计时纯数字滚动 ✅；token 过期白屏修复（api.ts 401 同步重置）✅
- 2026-09-02 新增：frp 内网穿透链路 ✅（远程文件访问出公网：47.116.59.141:3900 → 本机 3900，frps/frpc v0.61.1 双端自启，外网端口已验证连通）✅ 应用内远程文件服务开关已于 09-04 打开

- 核心模块（MVP）：登录认证 / 任务日程 / 饮食记账 / 钱包 / 对话 / 提醒 / 管理后台 / 社区 / 工作交接 / 实名认证 ✅
- Phase 2 已交付 22 / 22 项设计功能 ✅
- AgentCore 智能体核心 P0-P4+ 全量交付（2026-08-20）：单例入口 / 短回复拦截 / 4 大内置 Skill（记账·日程·心理·办公）/ 静默知识库 / @指令 / 隐私四档 / 周报 / 运营配置化 + A/B 实验埋点与统计 ✅；2026-08-24 TS 源码规范化 + 路由/调度器激活（src/services 4 文件 + index.ts 启动 + chat/admin 路由接入）✅
- QQ 式社交层（2026-08-21）：好友（请求/接受/备注/私聊）/ 群聊 / 朋友圈 / 相册 / 社区同步入口 ✅
- UI v5 黑白灰改版 + 实时天气横幅 + QQ 风底部 TabBar ✅
- 已完成 PWA 改造（可添加到主屏幕、离线访问）✅
- 已完成 Electron 桌面端打包（Win NSIS + Portable）⚠️（已知缺陷见第七节）
- ✅ LLM 已恢复可用（2026-08-24 E2E 验证 GLM-Z1-9B-0414 响应正常，7s 回复）；此前 2026-08-21 SiliconFlow 余额耗尽问题已解决

---

## 三、已完成功能清单（按模块）

### 3.1 用户与认证
- 注册 / 登录（JWT + bcrypt + 速率限制 10 req/min/IP）
- 冷启动画像（onboarding）
- 实名认证等级体系（Lv0-Lv5，SA-08 + SA-13 + AD-07 管理端审核）
- 头像上传（图片 base64 + Canvas 客户端压缩至 256x256，2MB 上限，写入 `auth.update_avatar` 台账）
- 个人资料更新（昵称 / 头像 / 本周目标 / AI 语气偏好）
- 注册不需要昵称（z.string().optional()，缺省回退用户名）

### 3.2 主页与事件总汇（EV-01 ~ EV-06）
- 主页今日概览（任务/账单/提醒/健康/事件多模块聚合，4 端点并行加载）
- 事件总汇 EV-01
- 事件冲突检测 EV-02（间隔 < 15min 自动告警）
- 事件卡片详情聚合 EV-03
- 智能准备助手 EV-04（任务前 3 天自动生成准备清单，7 模板）
- 情境视角切换 EV-05（全部 / 工作 / 生活 / 财务 四 Tab）
- 事件手动管理 EV-06（ManualEvent + HiddenEvent 双模型）

### 3.3 对话与 AI 助理（DG-01 ~ DG-16 + AgentCore）
- **AgentCore 智能体核心（2026-08-20 全量交付）**：
  - `backend/dist/services/agentCore.js` 单例，全部消息流走 `getAgentCore().handle(request)`；运行时：用户输入 → 短回复拦截 → LLM 首轮 → 工具并行 → LLM 二次总结 → 流式返回 + 后台并行理解管线入库
  - 短回复 5 类分支拦截：纯确认（silent_ack 不发气泡）/ 情绪停顿（极短共情）/ 模糊意向（代决策落地）/ 礼貌结束（温柔收尾）/ 噪音（30% 同款 emoji）
  - 4 大内置 Skill（Function Calling）：accounting（缺槽位不追问，pending 后台补全 + 自然语言查账）/ schedule（冲突检测顺延 + 模糊匹配销办 + 未来窗口查询）/ psychology（静默情绪评估 + 危机三级 + 认知重构 + 波动共情 + 人优先于事）/ office（read 摘要 / write 只读不改原件 / export 下载，docx 零依赖 ZIP 手解）
  - 静默知识库：每轮对话异步抽取实体/关系/偏好/事件/习惯/情绪 → Fragment；用户零感知；仅 @回顾 / @画像 / @找 三条显性化指令
  - 隐私四档 privacyMode（full / text / local / off）+ 记忆管理（筛选/改标签/撤销）+ 一键导出 Markdown（Obsidian 兼容）/ JSON
  - 周报：buildWeeklyReport 自然周聚合 + 每周一 03:00 weeklyAggregator 预聚合缓存 + WeeklyReportPage 前端页
  - 运营配置化：agentConfig.js 热更（语料池/A-B 分桶/碎碎念概率/人设）+ admin 端点 + AdminAgentConfigPage
  - A/B 实验：greeting 消息 metadata 快照 abBucket 埋点（零新表）+ abStats 聚合 + AdminAbStatsPage（24h 回复率/活跃/人均指标）
- 多模态消息 DG-02（图片/文件/位置，base64 内联，5MB 上限）
- 快速指令 DG-04（/ 唤出菜单，8 项快捷指令，键盘 ↑↓ 选择）
- 实时规划 DG-06（schedule_card，根据任务/睡眠/饮食生成 90 分钟焦点块）
- 碎片信息收集 DG-07（Fragment 模型 + 自动分类 note/link/todo/idea/snippet）
- 隔夜整合卡片 DG-08（digest_card，按日聚合碎片 + 标签云 + 待办清单）
- 跨信息关联 DG-09（connections_card + GET /fragment/insights 4 信号：orphanTodos / unactedIdeas / financeMentions / taskEchoes）
- 录音直达纪要 DG-10（VoiceMemo + MediaRecorder + 转写 mock + 待办提取，可一键转任务）
- 实时会议纪要 DG-11（LiveMeetingPage，60s 自动分段，50MB 上限，finalize 时合并上传）
- 每日一问 DG-05（topic 池 + dayOfYear 轮换保证同日稳定）
- 状态感知互动 DG-13（5 信号：late_night / task_overload / schedule_tight / no_sleep_record / overspend）
- 迷茫期引导 DG-15（7 天窗口 5 信号：task_stall / life_disconnect / self_care_missing / finance_disconnect / no_goal）
- 场景问候 DG-12（getSceneGreeting 返回 { title, subtitle, tip }）
- 成就系统 DG-14（todayProgress + streaks + badges，与 /home/today 聚合返回）
- 危机干预 DG-16（critical / high / moderate 三级检测 + 全国心理援助热线 400-161-9995 等）

### 3.4 工作与任务
- 任务 CRUD + 四级优先级（urgent/proper/normal/silent）
- 任务白名单校验（taskUpdateSchema，防 userId/id/status 覆写）
- 工作交接表（Handover 模型，draft/submitted/archived 状态流转，可导出 Markdown）

### 3.5 生活与健康
- 饮食记录（早午晚加餐 + 营养字段 + inputMode="decimal"）
- 睡眠分析基础版（自动算 durationMin + autoQuality 推断 + 周报 + 睡眠债）
- 提醒四级（reminderLevelMeta 集中常量）
- 本地小说阅读（2026-08-17 新增）：TXT/EPUB/PDF/DOCX/HTML/MD 六格式；书架 + 阅读器（`/novel`、`/novel/:id`）；文件存 IndexedDB（桌面端 `desktopAPI.cacheAsset`），元数据存 localStorage；进度记忆（TXT 章节索引+滚动比例 / EPUB CFI / PDF 页码）；阅读主题（白纸/米黄/护眼绿/夜间）+ 字号行距调节；入口三处：首页问候卡下方快捷卡片（不依赖后端数据，离线可用）、桌面侧边栏"生活"组、路由直达

### 3.6 财务与钱包
- 账单记账（Bill 模型，billDate 字段，分类自动识别 classifyBillCategory）
- AI 聊天账单卡片（bill_card，autoCategory 高亮 + 智能建议）
- 钱包（Wallet 懒创建 + 首次访问触发 wallet.auto_create 台账）
- 充值（单笔 ≤ 5000 元限制）
- 交易流水（Transaction + 关联订单号）

### 3.7 社区
- 发帖（无标题，类朋友圈体，life/work/finance 分类）
- 评论 / 点赞 / 收藏（联合唯一约束）
- 帖子级联删除（评论/点赞/收藏一并清除，admin.community_delete 台账）
- 朋友圈 / 相册同步入口（2026-08-21）：Tab 下第一卡 QQ 空间式快捷区，跳 /moments 与 /album

### 3.8 管理后台
- Dashboard（18 个独立查询合并 Promise.all 并行）
- 用户列表 + 详情（4 项统计并行）
- 实名审核（PATCH /admin/verifications/:id，admin.verify_review 台账）
- 交接单查看
- 社区内容审核与删除
- 操作台账（AuditLog，category + action + IP + UserAgent + durationMs）

### 3.9 搜索与其他
- 全局搜索 SearchPage（debounce 250ms）
- 健康检查 /health
- PWA（manifest.webmanifest + Service Worker + 离线缓存 + 桌面图标）
- Electron 桌面端（Win NSIS + Portable）

### 3.10 QQ 式社交层（2026-08-21 新增）
- **会话系统**（conversation.routes.js）：每用户独立 Conversation 列表（ai/user/group 三型）；AI 会话沿用 ChatSession/Message（保留 DG-* 卡片能力），用户私聊与群聊走 ConversationMessage；备注名 remark 覆盖昵称；未读数接收方累加；SSE 实时推送（/conversations/events，EventSource 用 query token）
- **好友管理**：双向 Friendship 模型（请求双向写入 pending，接受双向 accepted）；搜索用户（users/search 防枚举限 10 条）；添加（按 username + 可选备注）/ 接受 / 拒绝 / 删除（连带删双方私聊会话）/ 改备注（同步 Conversation.remark）；与好友开启私聊（POST /friends/:id/conversation 返回会话对象本身）
- **群组管理**：创建（owner）/ 邀请入群 / 退群与踢人（owner/admin）/ 更新群信息
- **前端**：ChatListPage「消息/好友」视图切换（待处理请求红点）；FriendsPanel 好友列表（搜索 + 字母分组 + 点击建会话进私聊 + 右键备注/删除）；AddFriendModal（搜索添加 / 待处理双 Tab）；CreateGroupModal（勾选好友建群）；ChatUserPage 私聊/群聊页（文本/图片/文件/位置消息）
- **朋友圈**（MomentsPage，/moments）：发表（文字 + 九宫格图片）、点赞 / 评论 / 全屏查看；顶部横幅毛玻璃回主页按钮
- **相册**（AlbumPage，/album）：多图上传、按月网格视图、滑动删除；Web 端路由已补注册（App.tsx 移动分支）
- **我的页**（ProfilePage）：QQ 空间式分区（白色圆角卡 + 4 列功能格）；9 位身份号 userIdCode（前端 31 哈希稳定）；余额隐藏只留钱包入口；提醒管理/饮食记录入口移除

---

## 四、本次会话（2026-09-08）工作节点

### 4.0 开机自启链路 + 用户数据恢复（2026-09-08）

- **问题发现**：项目迁移到 D 盘后，Startup 文件夹两个 vbs 被移除（仅剩 desktop.ini），vbs 源文件路径仍指向旧 C 盘桌面路径；`AppData\Roaming\aie-desktop` 用户数据目录整体丢失（aie.db / llm.env / agent-persona.txt / remote-access.json / JWT 密钥全部消失，回收站与存档 zip 均无备份）
- **恢复动作**：
  - vbs 路径更新：`frpc-tunnel.vbs` 指向 `D:\源码存档\助理项目\frp\...`；`start-backend.vbs` 的 nodePath 改 `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-2\node.exe`（`C:\Program Files\nodejs\node.exe` 已不存在）
  - ⚠️ **vbs 编码教训**：用文本编辑工具改 vbs 后会被重写为 UTF-8 且中文被破坏成"锟斤拷"，必须用 PowerShell 以 GBK(936) 编码整体重写。两个 vbs 已重新放回 Startup
  - 数据库重建：复制 `electron\resources\backend\prisma\dev.db`（空模板含 schema）到 `userData\data\aie.db`，**桌面端用户数据无法恢复，需重新注册**
  - 配置重建：`llm.env`（从 backend/.env 提取 LLM_* 系列）；`agent-persona.txt`（从 agent-config.json personaPrompt 恢复，621 字符）；`remote-access.json`（enabled:true / port 3900 / BYECPH / 4 目录）
  - ⚠️ **remote-access.json 两个坑**：① 字段是 `enabled/port/code/roots`（非 accessCode/allowDirs），roots 需绝对路径；② PowerShell 默认 UTF8 写入带 BOM，JSON.parse 失败→loadConfig 走默认值 enabled=false，必须用 `New-Object System.Text.UTF8Encoding($false)` 写
- **验收**：模拟开机（停全部进程 → wscript 触发两个 vbs → 等 75s）frpc 自动拉起 + 后端自动拉起，本机 3001/3900 + 公网 47.116.59.141:3900/3001 健康检查全部通过

---

## 五、历史会话（2026-08-20 ~ 2026-08-24）工作节点

### 4.1 重新注册账号 + 后端 dist 旧版本修复 + 自动备份（2026-09-08）

- **账号**：本机新注册 `lujiaoxi` / 密码 `Lujiaoxi2026`（首位注册自动成为 admin）。旧对话记忆确认无法恢复（本机库仅有测试账号，服务器数据缺 SSH 私钥/密码无法访问）
- **vault 500 bug 根因 + 修复**：electron/resources/backend 的 dist/schema/prisma-client 是**旧版本**（缺 VaultNote 模型），与 backend 源码不一致 → 查 vault 报 500。修复：backend `tsc` 重新编译 → dist 整包同步到 electron/resources/backend/dist → 同步 schema.prisma → `prisma generate` 重新生成 client → `prisma db push` 给运行库补 vault_notes 表（lujiaoxi 账号数据保留）
- **frpc.exe 丢失恢复**：`frp_0.61.1_windows_amd64\frpc.exe` 意外丢失，从 `frpc.zip` 解压恢复
- **自动备份机制新增**：`backup-aie.cjs`（复制整个 aie-desktop 到 `D:\源码存档\助理项目\备份\`，保留最近 5 份）+ Startup `backup-aie.vbs`（开机 60s 后执行，GBK 编码）
- **验收**：lujiaoxi 登录=admin，vault 接口 200，LLM DeepSeek-V3 可用（1s），本机 3001/3900 + 公网 3900 全通；备份脚本实测产出 `aie-desktop-20260908104338`
- **补充 1（res.json(success()) bug）**：`success()` 内部已 `res.send`；vault.routes(4处)+remoteAccess.routes(3处) 误写 `res.json(success(res,...))` → success 返回 res 对象再被 res.json 序列化 → 循环引用 500。修复为直接 `success(res, ...)` 并重编译同步
- **补充 2（vault 枚举+提示）**：vault 的 category 合法枚举为 `person/preference/event/timeline/other`，传入其他值 zod 校验失败返回 500（非 bug）；中文 title/content 存 UTF-8 正常，PowerShell 控制台显示 ???? 只是显示层问题，勿误判
- **补充 3（遗留）**：lujiaoxi 的 vault 已留 1 条真实记忆（工作偏好）；测试期间的脏记忆已清理

### 4.7 新账号远程文件访问验证（09-08）

- **访问配置**：enabled=true、running=true、端口 3900、访问码 **BYECPH**（本机起进程随机生成，重启会刷新）、授权白名单 roots=桌面/文档/下载/图片
- **验证通过项**：
  - H5 首页（本机+公网）200，页面 16056B
  - 访问码认证 `/auth`：错码返 401，正码返 12h HS256 令牌
  - 列目录 `/api/list?path=<绝对路径>`：Desktop 14 项（文件夹+文件），公网同样正常
  - 下载 `/api/file?path=`：6月预测.xlsx 14135B 与源字节一致
  - 上传 `/api/upload?dir=&x-filename=`：text 成功落盘，删除 `/api/delete`（需 confirm=true）后文件消失
  - 路径穿越（`..\\..\Windows`）被 safeResolve 拦截 400；上传扩展名黑名单生效
- **字段注意**：list 返回 **items**（非 files）；上传文件名用 **x-filename 头**（非 query filename）；AI 找文件 body 用 **message** 字段（200 字内）
- **注意事项**：AI 找文件 `/api/ai` 能响应（LLM 正常）但对中文口语命中一般，files 常为空，属语义匹配能力限制非故障；Token 每次重启失效需重输码（安全设计）

### 4.7.1 list 意图 bug 修复 + 中文语义能力评估（09-08）

- **中文语义匹配能力测试结论**：文件**搜索**很强（精确名/语义变体「预测类→6月预测.xlsx」/中英混合「xlsx表格」/数字中文变体「六月预测」/闲聊识别/无结果告知 全过）；但**目录浏览(list)意图曾全失效**
- **list bug 根因**：`parseIntent` 让 LLM 输出 `{"action":"list","dir":"Desktop"}`，但 `AiIntent` 接口**没有 dir 字段**，解析时丢弃 `dir` 只留 keywords → keywords 为空 → list 分支匹配不到根目录。表现：任何"显示桌面/打开下载"都返回「没找到这个目录」
- **修复（remoteFileServer.ts 3 处）**：① `AiIntent` 加 `dir?: string`；② parseIntent 返回时透传 `parsed.dir`；③ `/api/ai` list 分支 `(intent.dir || intent.keywords[0])` 匹配 roots
- **修复后回归**：4 种目录表达 3 种稳定命中（返回正确 dir），1 种「桌面文件都在哪」属 LLM 判定波动（3 次测 2 次正确列目录）；搜索/闲聊回归零回归
- **扩展名感知过滤（已实现）**：原「找pdf」子串匹配会混入 pdf.png/PDFSetup.exe 等（命中的是桌面真实存在的项目文件，roots 干净、非泄漏，早先"roots 暴露 webpack 产物"判断错误）。修复：新增 `EXT_KEYWORDS`（常见扩展名集合）+ `matchFile()`——关键词是扩展名时精确匹配 `结尾.扩展名`，否则子串匹配。实测「找pdf」30→10 纯 pdf 文档；「预测类」(非扩展名)仍子串命中 6月预测.xlsx；「xlsx」2 个纯 xlsx。均在授权范围内，语义更准
- **经验**：native Tool/源码修改需重编译 backend → 同步 electron/resources/backend/dist + schema + prisma client → db push → 重启；dist 是 electron 实际运行版

### 4.8 旧数据恢复调查 + 服务器 SSH 登录 + 自动备份核查（2026-09-09）

- **目标**：尝试恢复新账号 lujiaoxi 之前的旧对话记忆
- **服务器旧数据调查结果（最终结论）**：旧对话记忆**无法恢复**，非凭据/技术问题，而是**数据源已不存在**
  - `47.116.59.141`（当前公网后端，9/4 部署）：SSH 实测 re prod.db（`/root/backend/prisma/prod.db`）→ 记忆相关表全部为 0（vault_notes/chat_sessions/messages/user_habits = 0），仅发现 6 个空账号（昵称"用户1~5"），均系此前"注册探测"误建，**服务器上本无任何旧记忆**
  - `8.154.45.247`（2026-08 demo 版，旧数据所在）：端口 3001/80 全不通，**已彻底下线**，物理上无法访问
- **⚠️ 教训（『注册探测』污染服务器）**：曾通过 3001 `/api/v1/auth/register` 逐个试探用户名枚举——因注册冲突应返 422，误以为能探测；实则对新库每次都真实注册成功，**在服务器上误建了 6 个测试账号**。已在本次随 SSH 登录**全部删除（users 5→0）**，服务器库恢复干净。教训：禁止用注册接口探测用户名，会制造垃圾账号
- **服务器 SSH 凭据（本机运维速查补充）**：root 密码登录可用（非公钥），密码见项目机密位置；连接工具 `scripts/server-ssh/ssh-exec.cjs`（ssh2 密码认证，读 `AIDEV_SSH_HOST/USER/PASS` 环境变量）
  - **ssh-exec.cjs 已改造**：原仅支持 `argv[2]` 传单条命令（复杂 SQL/heredoc 引号必炸）。本次改为支持 **stdin 管道传完整脚本**（无 argv[2] 时读 stdin），彻底绕开 PowerShell 引号地狱。用法：`$script | node ssh-exec.cjs`
  - 运维速查补充：`sqlite3` 位于服务器 `/usr/bin/sqlite3`
- **待用户提供（继续推进才需要）**：找回/挂载旧 `8.154.45.247` 的镜像或 prod.db 导出 → 可用预置迁移工具导入
- **预置迁移工具（已备可复用，本次未用）**：
  - `sync-old-db.sh`（服务器端导出 prod.db 全表为 JSON dump，只读）
  - `migrate.cjs`（本机导入器：读 dump → 合并进 lujiaoxi，含主键冲突保护 `INSERT OR IGNORE`，不覆盖现有数据；支持 dry-run 预览）
  - **实测链路**：dry-run + apply 均通过（本机 aie.db 与服务器 prod.db schema 完全同构，两者 id 均为纯文本主键，可表级直接合并）
- **自动备份核查（生效确认）**：✅ 已产出 2 份备份证明自启链路真实运转
  - `备份\aie-desktop-20260908104338`（9/8 18:43）、`备份\aie-desktop-20260909011827`（9/9 09:18，今日开机自动生成）
  - 链路：Startup `backup-aie.vbs`（开机 60s 后执行）→ `backup-aie.cjs`（整目录拷贝 aie-desktop → `D:\源码存档\助理项目\备份`，跳过 cache，保留最近 5 份）
  - 后续每次开机都会自动备份，新账号记忆从 9/9 起有兜底，不会再丢

### 4.2 AI 人设落地（09-04 · 老朋友/隐形管家）

- **人设文本持久化链路（三重保障）**：① `AppData\Roaming\aie-desktop\agent-persona.txt` 存完整人设文本；② `AgentConfig.personaPrompt`（agent-config.json）同源写入；③ electron/main.cjs 每次启动读取 agent-persona.txt → 强制注入 `AGENT_PERSONA_PROMPT` 环境变量（后端 llmService 读取人设时环境变量优先级最高）→ 重打包/换目录/丢配置都能恢复人设
- **人设核心规则（铁律，新 AI 接手必须遵守）**：
  - 你是谁：老朋友/隐形管家，禁客套话、说教、客服腔；称用户"你"，自称"我"；禁"您/用户/建议您"
  - 主观选择题（"晚上吃啥"类）给 2-3 个带理由的选项让用户选，**不替用户拍板**
  - 客观事实问题直接短答，不硬凑选项
  - 影响大的操作（删除/花钱/改密码）**先确认再执行**
  - 模糊意向（随便/都行/你看着办）**代决策直接落地**并回报
  - 明确指令必落地成动作，信息缺失按最合理默认值执行，一句话回报，不反问不商量
  - 答案简短答完即止，不主动续话、不加客套结尾
  - 做不到的指令明说，不许装做
  - LLM 不得在 vault 为空时编造记忆（防幻觉）
- **人设生效验证三场景**：①"开个 6 点的倒计时"→ 直接办；②"晚上吃啥"→ 给选项；③"把我上周的任务都删了"→ 先确认
- **改人设注意**：修改 agent-config.json 的 personaPrompt 后需删除 userData\agent-persona.txt 并重启应用（agent-persona.txt 是首次提取缓存，不删会覆盖新配置）

### 4.2 自动化聊天统一"资料库 + LLM"生成（2026-09-04）
- **核心函数**：`proactive.lib.ts` 导出 `buildPersonalizedGreeting(userId, displayName, tone, now, todayTasks, extraContext?)`
  - 流程：vault 检索（`getVaultNotesForProactive` 置顶+最近优先，取 8 条）→ 拼今日待办摘要 → 系统提示含人设铁律 → LLM 生成 2-4 句 → 校验长度 1~200 字符 → 失败回退内置模板
  - **防幻觉**：`formatVaultBlock(notes)` 为空时系统提示第二行强制"禁止编造资料里没有的内容，只问候日常与待办"
- **双调度器复用**：ProactiveScheduler（每 5min 检查 8-10 点窗口主动问候）与 MorningGreeting（9 点）/vault 接口全部调用同一函数；`morningGreeting.lib.ts sendGreetingToUser` 增加 tone 参数 + 睡眠时长（<6h 关心 / ≥7h 表扬）+ 近 3 条负面情绪词检测（累/烦/压力/焦虑…）作为 extraContext
- **接口**：`POST /api/v1/chat/morning-greeting` 手动触发早安（传 preferredTone），返回 { success, messageId, content }
- 新增文件：`vaultService.ts getVaultNotesForProactive`；`proactive.lib.ts` 导出 TodayTask 接口

### 4.3 安卓端公网接入 + APK 重发（2026-09-04）
- **API 地址切换**：`frontend/.env.mobile` 的 `VITE_API_BASE_URL=http://47.116.59.141:3001/api/v1`（从本机 127.0.0.1:3001 切换）
- **构建链**：mobile 模式 vite build → `npx cap sync android`（同步最新前端含早安资料库模式）→ JDK 21（Microsoft OpenJDK 21，`C:\Users\Administrator\jdk-21`，JAVA_HOME 已配）gradlew assembleDebug（debug 签名，个人测试用）→ 输出 APK
- **APK 发布**：桌面 `绿角犀-安卓端-v2026.09.04.apk`（12.6MB；已解包核对内部 URL 为 47.116.59.141）
- **手机安装**：浏览器打开 `http://47.116.59.141:3900` → 访问码 BYECPH → 桌面目录 → 下载安装（需允许"未知来源"）
- ⚠️ 安卓端未完成：HTTPS、正式签名、超时重试、离线缓存、推送、应用锁等（详见 CHANGELOG 遗留，均属后续迭代项）

### 4.4 公网后端部署（47.116.59.141 上海，2026-09-04）
- **路径/进程**：`/root/backend`（源码+dist+node_modules+prisma）；PM2 进程 `aie-backend`（ecosystem.config.cjs：fork ×1、autorestart、max_memory_restart 500M、日志 logs/error.log + out.log）；`.env` 设 `HOST=0.0.0.0`（此前默认 127.0.0.1 导致公网不可达）
- **数据库**：Prisma 绝对路径 `file:/root/backend/prisma/prod.db`（相对路径会建错位置）；上线时已 `prisma db push` 补全 vault_notes 等新表
- **安全组**：sg-uf639nktwpt4rchwvkcc（实例 i-uf639nktwpt4rchwy1vg）新增入方向 TCP 3001（0.0.0.0/0，描述"绿角犀后端API"），经 OpenAPI 门户 AuthorizeSecurityGroup 调用完成（aliyun CLI 无凭证，控制台"增加规则"按钮无法自动化）
- **验证**：`curl http://47.116.59.141:3001/health` → {status:ok}；安卓端 API 通
- ⚠️ 服务器另有旧应用服务器 8.154.45.247（2026-08 部署 demo 版），两者独立，勿混

### 4.5 远程文件服务 + 开机自启（2026-09-04）
- **远程文件服务开关**：`AppData\Roaming\aie-desktop\data\remote-access.json` `enabled:true`（默认关闭）；端口 3900；访问码 BYECPH；安全白名单 4 目录（桌面/文档/下载/图片），越权 403（safeResolve）
- **链路**：手机 → 47.116.59.141:3900（frps 公网口）→ frp 隧道 → 本机 127.0.0.1:3900（后端 remoteFileServer）
- **开机自启（本机）**，启动文件夹 `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\`：
  - `frpc-tunnel.vbs` — 延迟 30s 拉 frpc（`助理项目\frp\frp_0.61.1_windows_amd64\frpc.exe -c frpc.toml`）
  - `start-backend.vbs` — 延迟 45s 拉 `node C:\...\APP-AIE\electron\start-backend-standalone.cjs`
  - ⚠️ 两个 vbs 都必须是 **GBK（ANSI/936）编码**——UTF-8 写中文路径会导致 wscript FileExists 判"不存在"直接退出（09-04 发现并修复，此 bug 曾让 frpc 一直无法开机自启）
- **引导脚本** `electron/start-backend-standalone.cjs`：复刻 main.cjs 注入（userData\llm.env → LLM_* ；agent-persona.txt → AGENT_PERSONA_PROMPT；DATABASE_URL=file:...\userData\data\aie.db）+ **幂等检查**（3001 已监听则静默退出，防与 Electron 双开抢端口）

---

## 五、历史会话（2026-08-20 ~ 2026-08-24）工作节点

### 4.1 AgentCore 智能体核心全量交付（P0-P4+，2026-08-20）
- **P0 核心框架 + 记账**：`backend/dist/services/agentCore.js` 单例 + 短回复 5 类拦截 + accounting 工具 + @指令 + 静默理解管线 + 启动问候；chat.routes 全部消息流走 `getAgentCore().handle()`；ChatPage 支持 silentAck + 新建会话即拉问候。验收：问候仅一次 / 记账快路 / silent_ack / 缺金额 pending / 纯数字补账 / 查账 30ms / @回顾 / @画像 / @找 全过
- **P1 日程 + 周报**：schedule 三件套（中文时间解析 + 同日 ±1h 冲突自动顺延 + 历史偏好兜底 / 标题模糊匹配销办 / 查未来 7 天窗口）；`buildWeeklyReport` 自然周聚合三端共用；`weeklyAggregator.js` 每周一 03:00 预聚合；`WeeklyReportPage.tsx` 四格统计页 + @回顾气泡跳转卡。验收全过
- **P2 心理 + 办公**：危机三级（critical 卡片+3 热线 / mid 心理陪伴 LLM 只接住不说教）；认知重构（第一人称自评 → 接住+换角度+60% 概率实证反驳）；波动共情（24h ≥2 条低落 → 占位，30 分钟冷却）；「人优先于事」（情绪负载跳过日程快路，记账照记）；office 三件套（read 3500 字分块摘要 / write 只读不改原件存 Fragment / export 下载 md|txt）；docx 零依赖 ZIP 手解 + PDF 尽力提取；`OfficeDocPage.tsx` 预览页。验收：API 冒烟 16/16 + 浏览器全流程
- **P3 画像 + 检索 + 隐私**：User 表 `privacyMode` 四档（full/text/local/off）管线拦截；`buildProfilePanel` 双端同源聚合；`ProfilePanelPage.tsx` 四格统计+情绪色带；记忆管理（GET /chat/fragments + PATCH/DELETE 属主校验）+ `PrivacyPage.tsx`；一键导出 md（Obsidian 兼容）/ json；@找 结构化结果。验收：API 冒烟 25/25 + 浏览器全流程
- **P4 运营配置化**：`agentConfig.js`（config/agent-config.json + DEFAULTS 兜底 + mtime 热更 + tmp/rename 原子写 + 校验 422）；A/B 分桶 `bucketOf(userId)`；admin 端点 GET/PUT /admin/agent-config + POST /admin/config/reload；`AdminAgentConfigPage.tsx`。验收：API 冒烟 23/23 + 浏览器全流程
- **P4+ A/B 埋点统计**：greeting metadata 快照 abBucket（零新表）；`abStats.js`（曝光按快照分桶、24h 回复率、活跃人均指标，老数据无快照丢弃计 legacySkipped）；GET /admin/ab-stats；`AdminAbStatsPage.tsx`。验收：API 冒烟 17/17 + 浏览器全流程
- **A/B 实验周报第 1 期**：`AB实验周报-第1期-2026-08-20.md`（A 池唠嗑风 vs B 池极简风、H1-H3 假设、判定标准曝光 ≥50/桶且回复率差 ≥10%、样本不足提示）；下周一出第 2 期

### 4.2 LLM 环境变更（2026-08-21，重要）
- SiliconFlow 账户余额耗尽（所有模型 402 Payment Required，含原免费 Qwen2.5-7B）→ agentCore 自动落入规则降级回复（机械句「中午，今天有 1 个待办」），即用户反馈「AI 回复不对」的根因
- `.env` 已预切 `LLM_MODEL=Qwen/Qwen3-8B`（实测输出质量优于 7B 且守人设），**充值后重启后端即生效**（dotenv 仅启动读一次）
- 诊断入口：GET /api/v1/chat/llm-status（需登录 token）
- 历史教训：Qwen2.5-7B `tool_choice:auto` 输出乱码（>10s），强制指定函数正常（<1s）；LLM 改写数字会口误 → 查账走确定性参数绑定

### 4.3 UI v5 黑白灰改版 + 实时天气横幅（2026-08-21）
- 全局配色 v5（作废 v4 浅蓝）：primary=slate 石板灰（index.css 明暗两套）；页面底 #F7F8FA；主操作渐变统一黑灰 `#334155→#1E293B`；彩色仅做数据语义点缀
- 主页横幅：useWeather 按天气码/昼夜切 /weather-bg 五图（晴/云/雨/雪/夜）全幅原色 + 顶部深晕 + 底部白渐变 + 毛玻璃天气小卡
- TabBar QQ 风：纯白实底+顶部分隔线；激活 slate-900 加粗+顶部 3px 黑圆角光条（300ms）；「我的」Tab 显示头像缩略图（激活黑描边）
- 头像更换完善：共享工具 `avatarChange.ts`（pickAndUploadAvatar → compressAvatarImage 256 方图 → POST /auth/avatar → updateUser 全局同步）；入口=个人页点头像 hover 相机遮罩 + 桌面顶栏下拉「更换头像」

### 4.4 QQ 式社交层 + 交互整合（2026-08-21）
- **朋友圈 / 相册**：MomentsPage（九宫格、点赞/评论/全屏）+ AlbumPage（按月网格、滑动删除）+ Web 路由补注册 + 毛玻璃回主页按钮；发帖测试数据已清理
- **消息间距修复**：ChatPage 中间层包装 div 隔断 space-y-3 导致消息间距 0，移到真正消息包装层恢复 12px
- **提醒并入待办**：RemindersPage 变兼容层（?float=1 桌面浮窗保留；普通访问重定向 /tasks?view=reminders）；ReminderListPanel 可嵌组件；TasksPage 待办/提醒胶囊切换；全部入口统一
- **我的页改版**：QQ 空间式分区（白色圆角卡 + 4 列功能格）+ 9 位身份号 userIdCode + 余额隐藏只留钱包 + 删提醒管理/饮食记录入口
- **对话好友功能**：ChatListPage「消息/好友」视图切换（待处理红点）；FriendsPanel（搜索+字母分组+点击建会话+右键备注/删除）；验收：好友全链路 API 冒烟 13/13（注册→搜索→请求→接受→列表→建私聊→会话可见→删好友连带删会话），测试账号已清理
- **社区页同步**：Tab 下第一卡朋友圈/相册快捷入口
- 编译验收：tsc --noEmit 零错误；浏览器实测全流程 console 无报错

### 4.5 AgentCore JS→TS 源码规范化 + 模块激活（2026-08-24）
- **源码规范化**：dist 下 4 个 JS 反推为 src TS（agentCore.ts ~1800 行 / abStats.ts 113 行 / weeklyAggregator.ts ~90 行 / proactive.lib.ts ~310 行），消除"tsc 重编即丢失 AgentCore"隐患；tsc strict 零错误
- **Schema 字段补齐**：User 加 privacyMode/proactiveChatEnabled，Message 加 isProactive（均有默认值，不破坏数据）；prisma db push 同步 + generate
- **调度器激活**：index.ts 启动 startProactiveScheduler（每 5min）/ startWeeklyAggregatorScheduler（周一 03:00）/ initAgentCore 单例预加载
- **chat 路由接入**：chat.routes.ts:170 generateReply() → getAgentCore().handle()；移除 proactiveSuggestions/contextSummary 顶层透传（合入 metadata，前端需改为从 metadata 提取）
- **/ab-stats 端点**：admin.routes.ts 新增 GET /admin/ab-stats?days=14
- **weeklyAggregator 查询修正**：User 无 messages 反向关系，改为 chatSessions→messages 链路
- **验收**：tsc EXIT_CODE=0；4 调度器启动无报错；E2E 注册→记账快路（0.5s 命中，wallet 写入）→fallback LLM（7s，GLM-Z1-9B）全链路通过；测试报告 backend/TEST_REPORT_AgentCore_Activation.md（10/10 + R1~R6 风险清单）
- **已知回归**：proactiveSuggestions/contextSummary 不再顶层透传（R1/R2，需前端联调）；chat 回复路径切换 LLM 7s 建议前端超时 ≥30s（R3）

---

## 六、历史会话（2026-08-19）工作节点

### 5.1 3D 宠物动态化 + 浮窗入口整合
- 重写 `frontend/src/components/pets/PetSprite.tsx`：CSS 3D 透视 + 7 套动作动画（idle/feed/play/clean/sleep/walk/pet）+ 4 种状态表情（happy/sleepy/hungry/sad）+ 随机眨眼 + 粒子效果
- `frontend/src/components/DesktopLayout.tsx`：侧边栏原 6 个浮窗按钮回退为 3 个
- `frontend/src/pages/TasksPage.tsx`：header 新增 monitor 图标按钮，点击展开下拉菜单收纳全部 6 类浮窗入口（pet/countdown/todo/reminder/sticky/wallpaper）

### 5.2 桌面端 UI 调整
- `DesktopLayout.tsx`：侧边栏改为纯深色（`bg-[#0A0A0F]` + `ring-white/[0.03]` 边缘高光）
- 所有圆角元素 `rounded-lg` → `rounded-xl`，导航项间距加大避免视觉拥挤
- hover/active 对比度增强以适配深色背景
- 构建通过：2366 模块 + 146 PWA 预缓存条目

### 5.3 随手记 deep link 集成 + Android Widget
- **Android 源码**：
  - `QuickNoteWidgetProvider.java`（AppWidgetProvider，点击发 `aie://quick-note` PendingIntent）
  - `widget_quick_note.xml` 布局 + `widget_quick_note_bg.xml` / `widget_icon_bg.xml` drawable + `widget_quick_note_info.xml` 元数据
  - `AndroidManifest.xml` 注册 intent-filter（scheme=aie, host=quick-note）+ widget receiver
  - `strings.xml` 新增 3 个字符串资源
- **前端 deep link 处理**：
  - `App.tsx`：`@capacitor/app` 的 `appUrlOpen` 监听，匹配 `aie://quick-note` → `navigate('/?quick=1')`；桌面端 `isDesktop()` 短路跳过
  - `HomePage.tsx`：`quickNoteRef` + `useEffect` 检测 `?quick=1` → 滚动到随手记卡片 + 自动聚焦输入框 + 清理 URL 参数
- **设计原因**：Android RemoteViews 不支持直接 EditText 输入，采用点击启动 app + deep link 聚焦输入框的模式
- **APK 状态**：`backend/data/app-release.apk` 仍为 2026-08-14 预构建版本，未含 widget；待 Android SDK + JDK 环境就绪时重建

### 5.4 桌面端 v15 重新打包
- 同步最新前端 dist 与后端（src/dist/node_modules/prisma）到 `electron/resources/`
- 复制 backend dev.db 作为模板，运行临时清理脚本归零所有业务表（users=0, auditLog=0）
- 修复 Electron 43 安装：`@electron/get` 为 ESM，需 Node ≥22；切换到 `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2` 后 `install.js` 成功下载 `electron.exe`
- `npm run dist:win` 输出：
  - `release-v15/绿角犀-Setup-1.0.0.exe`（191.49 MB，NSIS 安装版）
  - `release-v15/绿角犀-Portable-1.0.0.exe`（191.02 MB，绿色版）

### 5.5 工程目录整理（2026-08-19 上半场）
- 模块化目录结构整理（9 个模块目录 + 各含总纲.md）
- 合并重复文档：宠物系统 4→1、DAM 资产分类 4→1，删除 8 个源文档
- 清理 27 个冗余脚本（electron 11 + backend 16），保留 20 个活跃脚本
- 详见 `CHANGELOG.md` 2026-08-19 散乱脚本清理 + 重复文档合并两条

---

## 七、历史会话（2026-08-04）工作节点（归档）

### 6.1 全项目检查结果
- ✅ 后端 `npx tsc --noEmit` 通过（exit code 0）
- ✅ 前端 `npx tsc --noEmit` 通过（exit code 0）
- ✅ 全局搜索 `res.json(success(...))` 错误模式 → 0 处残留
- ✅ 全局搜索 `HttpError(400, '...')` 错误参数顺序 → 0 处残留
- ✅ 前端全局搜索 `: any` 类型 → 0 处残留

### 6.2 已修复的 22 处后端响应发送错误
- 问题：22 个路由文件使用 `res.json(success(res, ...))`，导致重复发送响应头 + 循环引用错误
- 修复：统一改为 `success(res, ...)`（success() 内部已 res.json）
- 影响：解决了服务器日志中循环引用 + headers already sent 报错
- 部署：已构建并部署至服务器

### 6.3 服务器数据库路径定位
- 问题：服务器存在两个数据库文件，旧路径与新部署不一致
- 确认正确路径：`/root/APP-AIE/backend/prisma/prod.db`
- 已修复 .env 中 DATABASE_URL 指向正确路径

### 6.4 用户反馈问题修复
- ✅ "任何日程无法新建"：前端拦截器仅接受 200，已改为接受 2xx（200-299）
- ✅ "增加更换头像功能"：POST /auth/avatar + ProfilePage 上传 + TabBar/HomePage/SettingsPage 多处展示
- ✅ "注册不需要写昵称"：后端 nickname.optional()，前端表单标"（选填）"
- ⚠️ PWA 旧缓存提示：用户需硬刷新（Ctrl+Shift+R）或清浏览器缓存加载最新版本

### 6.5 部署架构（已上线）
- 服务器：阿里云 ECS 8.154.45.247（root）
- Node.js v20.20.2 / PM2 7.0.3 / Nginx 1.24.0
- 后端：PM2 进程 `aie-backend` 监听 3001 端口
- 前端：Nginx 80 端口静态资源 + /api/ 反向代理到 3001
- 数据库：SQLite `/root/APP-AIE/backend/prisma/prod.db`
- 访问地址：http://8.154.45.247
- 演示账号：demo / 123456（普通用户）；admin / admin123（管理员）

---

## 八、已知问题与待办

### 7.1 已知缺陷
- ✅ **SiliconFlow LLM 余额耗尽**（2026-08-21 报告，2026-08-24 已恢复）：2026-08-24 E2E 验证 LLM 可用（GLM-Z1-9B-0414，7s 响应）。.env 现配置 LLM_PROVIDER=openai / LLM_MODEL=THUDM/GLM-Z1-9B-0414；诊断 GET /api/v1/chat/llm-status
- ✅ **AgentCore 服务器同步**（已解决 2026-09-04）：src TS 规范化 + dist 已随 47.116.59.141 公网后端整包部署（PM2 aie-backend）；本机 electron resources\backend\dist 同步一致。（旧 8.154.45.247 demo 服务器未升级，勿用）
- ✅ **Electron 桌面端启动失败**：已修复（2026-08-06）。根因：asar 打包导致后端文件不可访问 + 路径计算错误 + 缺少数据库自动初始化。修复方案：改用 extraResources 放前后端、修正路径、添加 prisma db push 自动建表。
- ✅ **PWA 旧缓存**：已修复（2026-08-06）。新增 PWAUpdatePrompt 组件，检测到新版本时底部弹出"发现新版本"提示，用户点击后调用 SKIP_WORKING + 页面刷新。同时每小时自动检查更新，首次安装显示"可离线使用"提示。
- ✅ **Android APK 未含 widget / 预构建过期**（已解决 2026-09-04）：jdk-21（C:\Users\Administrator\jdk-21）就绪后已 `npx cap sync android` + assembleDebug 重建，桌面 `绿角犀-安卓端-v2026.09.04.apk`（内置公网地址），widget 改动随最新前端一并包含（Android 源码在 frontend\android）。
- ⚠️ **安卓端正式版待办**：HTTPS（当前明文 HTTP）、正式签名（现为 debug 签名）、API 地址动态配置、超时/重试、网络切换监听、离线缓存（Service Worker）、深色/推送/应用锁/多语言等（长时间清单见 CHANGELOG 遗留，均未实施）
- ⚠️ **录音转写 mock**：`backend/src/lib/voice-memo.ts` 中 `transcribeAudio()` 是 MVP 占位实现，返回固定文本。生产环境需替换为真实 STT（Whisper / 阿里云 / 腾讯云）。
- ⚠️ **PDF 中文扫描件不可提取**（office.read_document）：已做友好报错；需要 OCR 能力再增强。
- ⚠️ **中文时间解析为规则式**（schedule）：复杂表达（"这周五之前"）不识别会掉 legacy；换强模型后可 LLM 口语化。
- ⚠️ **机器时钟比真实时间快约 8 小时**：DB UTC 时间戳 ≈ 本地显示时间 -16h；跨午夜判断（streak/问候时段）以机器本地时间为准，内部自洽，注意与真实日历对不上。
- ⚠️ **vbs 自启脚本编码约束**：`%APPDATA%\...\Startup\` 下两个 vbs 必须保持 **GBK 编码**；任何编辑器以 UTF-8 保存中文路径会导致自启失效（FileExists 判不存在退出）

### 7.2 待办 / 改进建议
- 接入真实 STT 服务替换 transcribeAudio()
- 语气模式实际生效（当前仅存储偏好，AI 回复未应用）
- 评估接入可穿戴设备数据（睡眠手环等），扩展 Sleep 数据来源
- 用户协议 / 隐私政策目前是占位 toast，需补充正式文本
- 小说书架多端同步：当前书架/进度仅本地存储（IndexedDB + localStorage），如需多端同步需设计后端书架元数据接口（文件本体过大不建议上传）
- EPUB 主题深度定制：当前主题作用于容器，EPUB 内部 CSS 可经 epubjs `themes.register` 进一步覆盖
- 大文件 TXT（>10MB）在低端移动设备解码+切章可能卡顿，可考虑 Web Worker 化
- A/B 实验持续观察：曝光 ≥50/桶后再下结论（自然攒样本需 3 个月+，可扩大触达）；第 2 期周报下周一产出
- 换强模型（32B+/Qwen3）后启用 LLM 工具路径：查账口语化、复杂时间解析、LLM 生成问候
- 长期：Android APK / iOS / macOS / Linux 桌面端 / 微信小程序覆盖

---

## 九、关键配置文件清单

| 文件 | 作用 |
| :--- | :--- |
| `backend/.env.example` | 环境变量模板（PORT / NODE_ENV / JWT_SECRET / DATABASE_URL / CORS_ORIGIN / ADMIN_*） |
| `backend/.env` | 实际环境变量（**禁止提交版本控制**） |
| `backend/ecosystem.config.cjs` | PM2 进程管理配置（aie-backend / 3001 / fork 模式 / autorestart） |
| `nginx.conf.example` | Nginx 反向代理配置（80 端口 / /var/www/aie-frontend / /api/ → 3001） |
| `deploy.sh` | 一键部署脚本 |
| `frontend/vite.config.ts` | Vite + vite-plugin-pwa 配置（manifest + workbox 自动生成） |
| `backend/prisma/schema.prisma` | 全量数据模型（17 张表，含 User/Wallet/Task/Diet/Sleep/Bill/Reminder/ChatSession/Message/Post/Comment/PostLike/PostFavorite/AuditLog/RealName/Fragment/ManualEvent/HiddenEvent/VoiceMemo/Handover/Countdown） |
| `frontend/src/lib/constants.ts` | 前端集中常量（categoryMeta / reminderLevelMeta / fragmentKindMeta / sleepQualityMeta / verifyLevelMeta / voiceMemoStatusMeta / eventScopeMeta / manualEventColorMeta） |
| `frontend/src/lib/api.ts` | axios 封装 + 拦截器（Bearer token + 2xx 通过 + 401 跳登录） |
| `electron/resources/backend/dist/services/agentCore.js` | AgentCore 智能体核心（单例入口 handle()：短回复拦截 / 4 大 Skill / @指令 / 理解管线 / 问候）——**真实部署位，backend/dist 是空壳** |
| `electron/resources/backend/dist/services/agentConfig.js` | 运营配置热更（config/agent-config.json：A/B 语料池 / 碎碎念概率 / 人设覆盖） |
| `electron/resources/backend/dist/services/abStats.js` | A/B 实验统计聚合（曝光快照分桶 / 24h 回复率 / 活跃人均） |
| `electron/resources/backend/dist/routes/conversation.routes.js` | 会话系统（好友 / 群聊 / 私聊 / SSE 推送 / 置顶免打扰备注） |
| `electron/resources/backend/config/agent-config.json` | 语料配置实文件（mtime 热更，PUT /admin/agent-config 写回） |
| `AppData\Roaming\aie-desktop\agent-persona.txt` | **AI 人设持久化实文件**（老朋友人设，electron/main.cjs 启动注入 AGENT_PERSONA_PROMPT；改 agent-config.json 的人设后需删此文件再重启） |
| `AppData\Roaming\aie-desktop\llm.env` | LLM 配置实文件（LLM_* 系列，启动注入环境变量，智能模式激活保障） |
| `AppData\Roaming\aie-desktop\data\remote-access.json` | 远程文件服务配置（enabled:true / port 3900 / 访问码 BYECPH / 白名单目录） |
| `AppData\Roaming\aie-desktop\data\aie.db` | **本机后端真实 SQLite 数据库**（electron resources\backend\dist 运行位读取） |
| `electron/start-backend-standalone.cjs` | **开机自启后端引导脚本**（复刻 main.cjs 注入 + 幂等检查，Startup\start-backend.vbs 调用） |
| `frontend/.env.mobile` | 安卓端 API 地址（VITE_API_BASE_URL=http://47.116.59.141:3001/api/v1） |
| `助理项目\frp\frpc.toml` | frpc 隧道配置（127.0.0.1:3900 → 47.116.59.141:3900） |

---

## 十、工程约定（硬性约束，修改前必读）

> 完整清单见本机 AI 记忆库：`C:\Users\Administrator\.trae-cn\memory\projects\-c-Users-Administrator-Desktop-------p2-7c1b09343a2e0390d628\project_memory.md`（新 AI 接手必读，含铁律、防幻觉、人设持久化等硬约束）

### 9.1 后端
- **所有写操作必须写台账**：`auditReq(req, res, { category, action, targetType?, targetId?, summary, detail? })` —— 对象形式，非位置参数
- **HttpError 构造**：`new HttpError(message, code = 400)` —— 消息在前，code 在后
- **success 函数**：`success(res, data, message?, code?)` —— res 必须是第一个参数
- **不能使用** `res.json(success(res, ...))` —— 会导致重复响应头
- **JWT_SECRET 必须环境变量**，禁止硬编码兜底值
- **登录/注册速率限制**：10 req/min/IP
- **录音上传速率限制**：5 req/min/user（基于 userId）
- **多模态消息速率限制**：10 req/min/user
- **钱包懒创建**首次访问触发 `wallet.auto_create` 台账
- **管理员不可降级自己角色**
- **充值单笔 ≤ 5000 元**
- **独立查询用 Promise.all 并行**
- **Bill 字段是 billDate，不是 date**
- **Prisma 模型是 prisma.diet / prisma.sleep，不是 dietRecord / sleepRecord**
- **admin 路由必须验证身份 + admin 角色**
- **NODE_ENV=production 避免错误栈泄露**
- **.env 必须加入 .gitignore**

### 9.2 前端
- **拦截器接受所有 2xx**：`data.code >= 200 && data.code < 300`
- **禁止 `any` 类型**，必须显式 interface 或 type assertion
- **领域常量集中** `src/lib/constants.ts`，禁止本地重复定义
- **确认对话框** 必须用 `useConfirm` hook，禁止 native `confirm()`
- **加载/错误/空状态** 用标准化 `LoadingState/ErrorState/EmptyState` 组件
- **搜索输入防抖** 用 `useDebounce` hook（250ms）
- **数字输入** 用 `inputMode="numeric"` 或 `"decimal"`
- **交接数据可导出 Markdown**
- **已完成任务/提醒** 视觉区分（opacity / line-through）
- **列表页四态渲染**：skeleton → error+retry → empty → data
- **新页面用 `React.lazy` + Suspense**
- **URL ?new=1 自动打开新增 Modal**
- **全局 ErrorBoundary** 包裹应用根

### 9.3 AI 助理模块（DG-07 ~ DG-16 + AgentCore）
- **AgentCore 新约定（2026-08-20 起生效）**：
  - 所有 HTTP 对话端点必须走单一入口 `agentCore.handle(request)`，禁止绕过自建 ad-hoc API
  - 人设 / 问候 / 周报模板 / System Prompt 改动一律走配置文件（agent-config.json 热更），禁止硬编码
  - 新能力必须注册为 Tool（Function Calling，命名 `{skill}.{action}`），禁止临时接口
  - 人设红线：不客套不说教不装专业；禁用词 您/用户/欢迎回来/检测到/建议您/为您服务/有什么吩咐
  - 短回复不发气泡（silent_ack）；psychology 仅高风险才显性共情；accounting 缺槽位绝不追问
  - A/B 埋点断言用消息 metadata.abBucket 快照（历史准确），活跃统计用当前 bucketOf 分桶——双轨口径勿混
- 未匹配的对话消息必须自动存为 Fragment（DG-07 默认路径）
- POST /fragment/digest 幂等：已消化碎片重跑返回空 summary，不报错
- PATCH /fragment/:id 用动态 SET 子句（仅显式传入的字段更新）
- DG-09 AI 触发关键词：`/跨模块|跨信息|洞察|未闭环|insights?/`
- DG-09 generateInsights() 在 fragment.routes.ts（REST）和 ai-reply.ts（chat）两处实现，修改时同步
- DG-16 危机检测必须最先执行（在所有匹配前），仅 contentPreview（前 10 字符）+ level 入台账，不入完整消息
- DG-16 不得生成鼓励/指导自伤内容，仅检测 + 引导专业资源

---

## 十一、本地开发命令速查

```powershell
# 设置 Node 路径（PowerShell）
# 注意：2026-08-17 实测 .trae-cn 下 node-v24.14.0 路径在本机已不可用；
# 2026-08-21 实测本机 PowerShell 的 PATH 里无 node/npx，用绝对路径最稳：
& "C:\Program Files\nodejs\node.exe" <脚本>
# 备选：C:\Users\Administrator\node-portable\node-v20.18.0-win-x64
#       C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2

# ⚠️ 本机后端真实部署位（AgentCore 所在，端口 3001 + 远程文件 3900）——改 dist 后必须重启才生效
# 推荐启动方式（开机自启同款，自动注入人设/LLM/数据库）：
& "C:\Program Files\nodejs\node.exe" "C:\Users\Administrator\Desktop\助理项目\助理项目\APP-AIE\electron\start-backend-standalone.cjs"
# 数据库：AppData\Roaming\aie-desktop\data\aie.db（非 electron/resources/backend/prisma/dev.db）
# 改 .env/人设后必须重启（dotenv 只在启动读一次）

# 后端源码开发（backend/dist 是空壳，仅源码参考）
cd c:\Users\Administrator\Desktop\助理项目\助理项目\APP-AIE\backend
npx prisma generate
npx prisma db push          # 同步表结构
npx tsx watch src/index.ts  # 热重载开发
npx tsc --noEmit            # 类型检查
npx tsc                     # 构建 dist/

# 前端开发
cd c:\Users\Administrator\Desktop\助理项目\助理项目\APP-AIE\frontend
npm run dev                # Vite 开发（5173）
node node_modules/typescript/bin/tsc --noEmit   # 类型检查（npx 不可用时）
npm run build              # 构建 dist/

# 类型检查一键脚本
cd c:\Users\Administrator\Desktop\助理项目\助理项目\APP-AIE
.\check.bat

# 桌面端打包
node build-desktop.cjs

# AgentCore 相关冒烟（backend/package.json scripts，cwd=backend）
npm run smoke:office   # 16 用例
npm run smoke:p3       # 25 用例
npm run smoke:p4       # 23 用例
npm run smoke:ab       # 17 用例
```

---

## 十二、服务器运维速查

### 11.1 应用后端服务器（8.154.45.247）

```bash
# SSH 登录（已配置公钥）
ssh root@8.154.45.247

# PM2 操作
pm2 status
pm2 logs aie-backend --lines 100
pm2 restart aie-backend
pm2 reload aie-backend

# Nginx 操作
systemctl status nginx
nginx -t
systemctl reload nginx

# 数据库
sqlite3 /root/APP-AIE/backend/prisma/prod.db
.tables
SELECT COUNT(*) FROM users;

# 查看日志
tail -f /root/APP-AIE/backend/logs/out.log
tail -f /root/APP-AIE/backend/logs/error.log
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# 健康检查
curl http://127.0.0.1:3001/health
curl http://8.154.45.247/health
curl http://8.154.45.247/api/v1/auth/me   # 应返回 401
```

### 11.2 frp 穿透服务器（47.116.59.141，华东2 上海，2026-09-02 新增）

```bash
# SSH 登录（已配置公钥 C:\Users\Administrator\.ssh\id_ed25519）
ssh root@47.116.59.141

# frps 服务（systemd 开机自启）
systemctl status frps
systemctl restart frps
cat /etc/frp/frps.toml        # bindPort=7000, auth.token 见文件

# 安全组（控制台 sg-uf639nktwpt4rchwvkcc）
# 已放行入方向：TCP 7000（frp 通道）、TCP 3900（远程文件穿透口）
# 注意：服务器 aliyun CLI 未配凭证；改安全组走控制台或 OpenAPI 门户在线调试

# 连通性验证（本机 PowerShell）
Test-NetConnection 47.116.59.141 -Port 7000
Test-NetConnection 47.116.59.141 -Port 3900
```

**本机 frpc 客户端**：
- 程序：`助理项目\frp\frp_0.61.1_windows_amd64\frpc.exe`，配置 `助理项目\frp\frpc.toml`（隧道名 aie-remote-file：127.0.0.1:3900 → 服务器 3900）
- 自启：`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\frpc-tunnel.vbs`（静默启动）
- 日志：后台运行查看命令输出；异常重连直接重跑 vbs 或命令行带 `-c` 参数启动

**远程文件访问入口**：手机浏览器 `http://47.116.59.141:3900`，访问码 BYECPH（应用设置→远程文件访问开关须打开，白名单目录：桌面/文档/下载/图片）

### 11.3 绿角犀公网后端（47.116.59.141，2026-09-04 部署）

```bash
ssh root@47.116.59.141
# 后端目录 /root/backend（源码、dist、node_modules、prisma、logs）
pm2 status                 # 进程 aie-backend
pm2 logs aie-backend --lines 100
pm2 restart aie-backend    # 改后端代码/环境后重启（环境变量注入重启才生效）
sqlite3 /root/backend/prisma/prod.db   # 服务器数据库（vault 等新表已 db push）
# .env 关键：HOST=0.0.0.0（公网 3001 可达）；DATABASE_URL=file:/root/backend/prisma/prod.db（绝对路径）
# 公网健康检查
curl http://47.116.59.141:3001/health
```
- **服务器 prod.db 备份存档（2026-09-09）**：`/root/backend/prisma/prod.db.bak-20260909`（本次调查前）与 `prod.db.after-inspect`（清理误建账号后）两份生产库备份，已 sftp 下载到本机 `D:\源码存档\助理项目\服务器存档\prod-db-备份\`（文件名 `prod.db-20260909-完整备份`、`prod.db-after-inspect`，各 1011712 字节，内容已实测无旧记忆数据）。本地拉取工具：`scripts/server-ssh/sftp-download.cjs`（ssh2 sftp fastGet，用法 `AIDEV_SSH_PASS=xxx node sftp-download.cjs <远端> <本地>`）

**安装包公网下载（2026-09-10 更新，三个安装包均已上传 `/usr/share/nginx/html/apk/`，经直连 IP 均可下载）**：
- 桌面安装版：`https://47.116.59.141/apk/绿角犀-Setup-1.0.0.exe`（159,356,517 字节）
  - 编码：`https://47.116.59.141/apk/%E7%BB%BF%E8%A7%92%E7%8A%80-Setup-1.0.0.exe`
- 桌面便携版：`https://47.116.59.141/apk/绿角犀-Portable-1.0.0.exe`（158,866,882 字节）
  - 编码：`https://47.116.59.141/apk/%E7%BB%BF%E8%A7%92%E7%8A%80-Portable-1.0.0.exe`
- 安卓 APK：`https://47.116.59.141/apk/绿角犀-安卓端-v1.0.1-正式签名.apk`（13,188,164 字节）
  - 编码：`https://47.116.59.141/apk/%E7%BB%BF%E8%A7%92%E7%8A%80-%E5%AE%89%E5%8D%93%E7%AB%AF-v1.0.1-%E6%AD%A3%E5%BC%8F%E7%AD%BE%E5%90%8D.apk`
- 服务器文件目录：`/usr/share/nginx/html/apk/`（基础 `greenrhino-cloud-ssl.conf` 443 默认站 `location /apk/` root 该目录）
- 证书：自签名（greenrhino.crt），首次访问需点"继续前往"
- 上传工具：`scripts/server-ssh/sftp-upload.cjs`（ssh2 sftp fastPut，用法 `AIDEV_SSH_PASS=xxx node sftp-upload.cjs <本地> <远端>`）
- 其它端口可达：443/18443/8443/3001 公网放行；80/3900/8080 被安全组拦截
- 其它电脑启用 AI：见第十三节 2026-09-09「桌面端『别的电脑启用 AI』交付方案」

**⚠️ 域名 `lujax.fun` 直连 IP 之外不可用（2026-09-10 诊断，未 ICP 备案 → SNI 封锁）**：
- **务必用上面的直连 IP 链接分发**；`https://lujax.fun/apk/...`（及该域名任何端口 443/8444）在本机网络出口均 TLS 握手 `Connection reset`。
- 根因：`lujax.fun` 未 ICP 备案，本机所在网络出口对 SNI=`lujax.fun` 做重置（SNI 过滤）。源站 TLS 健康（服务器本地 `openssl s_client -servername lujax.fun` Verify 0 ok，nginx -t / reload 正常）；强制 `--resolve lujax.fun→47.116.59.141` 仍重置、而直连 IP（不带该域名 SNI）正常返 206 ⇒ 属运营商/主干网域名封锁层，服务器与 Cloudflare 端均无可修复项。
- 已做（不改变上述可用性）：为 `lujax.fun` :443 站点（`lujax-cloud-https.conf`）新增 `location /apk/` 静态托管，`nginx -t` 通过、`reload` 成功，服务器内 `Host: lujax.fun` 请求 `/apk/...` 返 200；原文件已备份为 `lujax-cloud-https.conf.bak.apk-location-20260910`。
- 结论：治本需为域名完成 ICP 备案（或换已备案域名）；治标即本节的直连 IP 链接。

**多用户云端账号激活方案部署（2026-09-09，上架激活架构升级）**：
- 云端角色：唯一身份签发方 + AI 网关。桌面打包版不再持有 LLM key，`/auth/*` 代理到云端、AI 走云端网关，密钥永不下发
- RSA 密钥对：私钥 `D:\源码存档\助理项目\服务器存档\cloud-jwt-private.pem`（仅云端，勿分发）；公钥 `electron/resources/backend/cloud-jwt-public.pem`（随安装包分发，只能验签不能伪造）；同源已用 Node crypto 核验
- 云端 `.env` 新增：`JWT_PRIVATE_KEY`（RS256 签发）、`JWT_PUBLIC_KEY`（RS256 验签）、`AUTH_MODE=local`（云端是签发方，非 proxy）；`.env` 中多行 PEM 用双引号包裹字面换行，dotenv 16 可逐字节还原（已实测）
- 云端数据库：`prisma db push` 已给 User 表加 `aiEnabled/aiPlan/aiExpiresAt`；admin 控制台 `PATCH /api/v1/admin/users/:id/ai` 开通/停用（带台账 user_ai_control）
- AI 网关：`POST /api/v1/ai/proxy`（gatewayAuth RS256 验签 + rateLimit 60/min + llmProxyCompletion 调 siliconflow/DeepSeek-V3）；未开通用户 403「AI 服务未开通」
- 公网端到端验证（2026-09-09）：注册→RS256 token→桌面公钥验签→admin 开通→AI 网关真实回复→普通用户 403，全链路通过；测试账号（e2eadmin/e2euser）已清理，prod.db 回 0 用户（首位注册自动 admin）
- 部署/运维命令：SSH 用本机公钥 `C:\Users\Administrator\.ssh\id_ed25519`（`ssh root@47.116.59.141`，无需密码）；执行器 `scripts/server-ssh/ssh-key.cjs`（私钥认证版，用法同 ssh-exec.cjs）；上传 `scripts/server-ssh/sftp-key-up.cjs`（目录递归）；改后端后 `pm2 restart aie-backend --update-env`

### 11.4 本机链路自检（手机下载不可用排查顺序）

1. `curl http://127.0.0.1:3900/health` — 本机后端 remoteFileServer 是否在跑（不在→跑 Startup\start-backend.vbs 或直接 node start-backend-standalone.cjs）
2. `curl http://47.116.59.141:3900/health` — 隧道是否通（不通→frps/systemd 状态 + 本机 frpc 进程）
3. 浏览器开 `http://47.116.59.141:3900` 输访问码 BYECPH — 403=服务开但白名单外；401=访问码过期

---

## 十三、版本节点记录

| 时间 | 节点 | 内容 |
| :--- | :--- | :--- |
| 2026-08-04 14:18 | 服务器首次部署上线 | Node v20.20.2 / PM2 7.0.3 / Nginx 1.24.0 / SQLite / 演示账号 admin+demo |
| 2026-08-04 15:06 | PWA 改造完成 | manifest + Service Worker + 桌面图标 + 离线访问，覆盖 Web/PWA/Windows 桌面端 |
| 2026-08-04 15:30 | 头像上传功能上线 | POST /auth/avatar + Canvas 压缩 + 多位置展示 |
| 2026-08-04 15:41 | 日程新建 bug 修复 | 前端拦截器接受 2xx，解决 201 状态码被拒 |
| 2026-08-04 15:44 | 注册昵称改选填 | 后端 z.string().optional()，前端标"（选填）" |
| 2026-08-04 23:44 | 全项目检查 + 22 处响应 bug 修复 | 后端 22 个路由 res.json(success(...)) 改为 success(...)，类型检查全部通过，已重新部署 |
| 2026-08-06 17:30 | Electron 桌面端启动修复 | 修复 3 个问题：① asar 打包改用 extraResources ② 路径计算修正 ③ 添加 prisma db push 自动初始化数据库。Win-NISIS + Portable 重新打包验证通过 |
| 2026-08-06 18:10 | PWA 版本更新提示功能上线 | 新增 PWAUpdatePrompt 组件，检测到新版本时底部弹窗提示用户点击更新；首次安装显示"可离线使用"；每小时自动检查更新。解决旧缓存用户看到旧版本问题 |
| 2026-08-17 | 离线模式上线 | 全局网络状态检测（stores/network.ts）+ OfflineBanner 横幅；auth 离线兜底（不再踢登录页）+ Capacitor token 持久化；离线静默心跳。断网可打开软件，本地功能（宠物/便签/日历/小说）不受影响 |
| 2026-08-17 | 本地小说阅读功能上线 | TXT/EPUB/PDF/DOCX/HTML/MD 六格式；书架 `/novel` + 阅读器 `/novel/:id`；IndexedDB 文件存储 + localStorage 元数据；分格式进度记忆（章节/CFI/页码）；阅读主题四套；依赖新增 epubjs/pdfjs-dist/mammoth |
| 2026-08-17 | 小说入口接入首页 | HomePage 问候卡下方新增常驻快捷卡片（不依赖后端数据、离线可用）+ 桌面侧边栏"生活"组"小说阅读"项；tsc 0 错误、浏览器实测跳转正常 |
| 2026-08-19 | 3D 宠物动态化 + 浮窗入口整合 | 重写 PetSprite.tsx（CSS 3D 透视 + 7 动作 + 4 表情 + 粒子）；侧边栏 6 浮窗按钮回退为 3，TasksPage header 新增 monitor 图标下拉菜单收纳全部 6 类浮窗入口 |
| 2026-08-19 | 桌面端侧边栏深色化 + 圆角升级 | DesktopLayout 改为纯深色（#0A0A0F + ring-white/[0.03]）；rounded-lg → rounded-xl；导航项间距加大；hover/active 对比度增强 |
| 2026-08-19 | 随手记 deep link 集成 + Android Widget 源码 | 新增 QuickNoteWidgetProvider（点击发 aie://quick-note）；AndroidManifest 注册 intent-filter + receiver；App.tsx 监听 appUrlOpen → navigate('/?quick=1')；HomePage 检测参数自动聚焦随手记输入框。APK 待 SDK 重建 |
| 2026-08-19 | 桌面端 v15 重新打包 | 同步最新前后端到 electron/resources/；dev.db 模板归零（users=0, auditLog=0）；修复 Electron 43 安装（@electron/get 需 Node ≥22，切到 .workbuddy 22.22.2）；输出 绿角犀-Setup-1.0.0.exe（191.49 MB）+ 绿角犀-Portable-1.0.0.exe（191.02 MB） |
| 2026-08-20 | AgentCore 智能体核心全量交付（P0-P4+） | agentCore.js 单例 + 短回复拦截 + 4 大 Skill（记账/日程/心理/办公）+ 静默知识库 + @指令 + 隐私四档 + 周报 + 运营配置化 + A/B 埋点统计；冒烟 office 16/16、p3 25/25、p4 23/23、ab 17/17 |
| 2026-08-20 | A/B 实验启动 + 第 1 期周报 | A 池唠嗑风 vs B 池极简风上线分桶；AB实验周报-第1期-2026-08-20.md（三假设 + 判定标准曝光 ≥50 且差 ≥10%）；下周一第 2 期 |
| 2026-08-21 | UI v5 黑白灰改版 + 实时天气横幅 | primary=slate 黑白灰主调 + 彩色仅数据语义点缀；主页天气五图横幅（深晕+白渐变+毛玻璃小卡）；头像更换共享工具 avatarChange.ts |
| 2026-08-21 | TabBar QQ 风改版 | 纯白实底 + 激活 3px 黑圆角光条（300ms）+ 「我的」Tab 头像缩略图 |
| 2026-08-21 | LLM 余额耗尽诊断 | SiliconFlow 全模型 402 → agentCore 规则降级（"AI 回复不对"根因）；.env 预切 Qwen3-8B 待充值；诊断 GET /chat/llm-status |
| 2026-08-21 | QQ 式社交层 + 交互整合 | 朋友圈/相册页（Web 路由补注册）；消息间距修复；提醒并入待办（TasksPage 双视图）；我的页 QQ 空间式改版 + 9 位身份号；对话好友功能（消息/好友切换 + FriendsPanel，冒烟 13/13）；社区页朋友圈/相册同步入口 |
| 2026-08-24 | AgentCore JS→TS 规范化 + 模块激活 | dist 4 JS 反推 src TS（agentCore/abStats/weeklyAggregator/proactive.lib）+ schema 补 privacyMode/proactiveChatEnabled/isProactive + index.ts 启动调度器 + chat.routes 接入 getAgentCore().handle() + admin /ab-stats 端点；tsc 零错误；E2E 10/10 通过 |
| 2026-09-01 | Obsidian 服务端记忆库（vault）上线 | vault_notes 表（userId 隔离 + 5 分类）+ 对话后 DeepSeek 异步提炼入 vaultService.ts + 对话时关键词检索注入 llmService.ts + 设置页 VaultPage 管理；安全四件套（JWT/隔离/限流/JSON 白名单） |
| 2026-09-01 | 浮窗整合 + 便签浮窗升级 + 倒计时改版 | 待办钉桌面改虚线全宽按钮；倒计时详情/列表页补 monitor 浮窗入口；便签卡 16px 圆角 + 毛玻璃工具钮 + 边缘缩放 + 大小记忆；CountdownDetailPage 纯数字滚动计时 |
| 2026-09-01 | token 过期白屏修复 | 401 时内存登录态未清导致登录页↔首页死循环白屏；api.ts L11-19 同步重置登录态；应用重打包重启验证通过 |
| 2026-09-02 | frp 内网穿透链路交付 | frps v0.61.1 部署 47.116.59.141（systemd 自启）；安全组放行 TCP 7000/3900（经 OpenAPI 门户 AuthorizeSecurityGroup 接口）；本机 frpc 隧道 127.0.0.1:3900→服务器 3900 + 开机自启 vbs；外网双端口连通验证通过。⚠️ 遗留：应用内远程文件开关待用户开启（remote-access.json enabled:false） |
| 2026-09-04 | AI 人设落地（老朋友/隐形管家） | agent-persona.txt 持久化 + AGENT_PERSONA_PROMPT 启动注入 + agent-config.json personaPrompt 三源；铁律⑨（主观选择题/客观短答/代决策/先确认）；验证三场景通过 |
| 2026-09-04 | 自动化聊天统一资料库+LLM | buildPersonalizedGreeting 通用函数（vault 检索 + 待办 + 睡眠/情绪 extraContext + 防幻觉）；ProactiveScheduler 与 MorningGreeting 双调度器复用；chat.routes 传 preferredTone |
| 2026-09-04 | 公网后端部署（47.116.59.141） | /root/backend + PM2 aie-backend + HOST=0.0.0.0 + Prisma 绝对路径 prod.db + schema 补全；安全组放行 TCP 3001（OpenAPI 门户 AuthorizeSecurityGroup）；公网 /health 验证通过 |
| 2026-09-04 | 安卓端切公网地址 + APK 重发 | .env.mobile → 47.116.59.141:3001；mobile build + cap sync + JDK 21 构建；桌面 绿角犀-安卓端-v2026.09.04.apk 发布，远程下载链路验证通过 |
| 2026-09-04 | 远程文件服务开关打开 | remote-access.json enabled:true（BYECPH / 3900 / 白名单4目录）；手机端首页→令牌→列表→下载全链路 200 |
| 2026-09-04 | 开机自启配置 + vbs 编码修复 | Startup 新增 start-backend.vbs（45s）+ 引导脚本 start-backend-standalone.cjs（幂等检查）；frpc-tunnel.vbs 30s；两 vbs 统一 GBK 编码（修复 UTF-8 中文路径致 FileExists 失败的隐藏 bug） |
| 2026-09-09 | 旧数据恢复调查 + 服务器 SSH 登录 + 自动备份核查 | 实测 prod.db 无旧记忆（8.154 旧服务器已下线）→ 旧对话无法恢复；SSH 密码登录成功并清理注册探测误建的 6 账号；ssh-exec.cjs 支持 stdin 管道；备份已产出 2 份证明自启生效；预置 migrate.cjs/sync-old-db.sh 迁移工具链 |
| 2026-09-09 | 桌面端安装级数据库初始化验证 + 健壮性修复 | 实机安装 Setup 并启动验证：全新首启从 resources/backend/prisma/dev.db 复制空模板建库（50 表 0 数据）、health 200。发现并修复缺陷：isDbInitialized 原只校验 .db-version 标记、不校验 db 文件存在，db 被删但标记残留会启动空库报 P2021 → 现强制校验 db 文件存在且非空。已重打包（release-v16 Setup+Portable v1.0.0 15:55） |
| 2026-09-09 | 桌面端「别的电脑启用 AI」交付方案 | 决策：LLM key 不随安装包分发（安全），改为独立交付 `release-v16/llm.env`（从源码 backend/.env 提取 LLM_*）。别的电脑启用方法：装包→首启注册→退出→把 llm.env 复制到 `C:\Users\<名>\AppData\Roaming\aie-desktop\`→重启即激活。依据 ensureLLMConfig 优先读 userData/llm.env，不会覆盖 |
| 2026-09-09 | 多用户云端账号激活方案（上架激活，替代 llm.env 分发） | 决策：改走「本地数据 + AI 走云端网关」。云端（47.116.59.141）为唯一身份签发方 + AI 网关：①RSA 密钥对（私钥 `服务器存档/cloud-jwt-private.pem`，公钥内置安装包 `electron/resources/backend/cloud-jwt-public.pem`，同源已核验）②云端 .env 注入 `JWT_PRIVATE_KEY/JWT_PUBLIC_KEY/AUTH_MODE=local`，RS256 签发 ③桌面打包版走 `AUTH_MODE=cloud-proxy`（只验不签，/auth/* 代理到云端 3001，懒同步云端 userId 到本地库）④User 表加 `aiEnabled/aiPlan/aiExpiresAt`，admin `PATCH /admin/users/:id/ai` 控制开通，网关按 aiEnabled 判权（false→403）⑤云端已有 LLM key（siliconflow/DeepSeek-V3），`POST /api/v1/ai/proxy` 网关代理。已部署 + 公网端到端验证通过（注册→公钥验签→开通→AI 回复→未开通 403）。测试账号已清理（prod.db 回 0 用户，首位注册自动 admin）。新安装包 release-v16 已重打包（Setup+Portable 21:05） |
| 2026-09-09 | 云端部署 + 测试账号清理 | 通过本机公钥 SSH（`C:\Users\Administrator\.ssh\id_ed25519`）连 47.116.59.141：备份 dist/prod.db → 推新 dist（tar 上传+解压）→ .env 注入 RS256 密钥 → `prisma db push` 加 AI 三列 → 重启 PM2 aie-backend。E2E 注册 e2eadmin(admin)/e2euser(user) 验证后已删除，prod.db 归零 |
| 2026-09-10 | 管理后台独立云端化 + 桌面端 release-v16 重打包 | 主 APP 删除 admin 路由与 LoginPage 两处入口链接；新增 admin.html + admin-main.tsx + admin-vite.config.ts 独立构建（publicDir:false 置于顶层）；后端 index.ts 托管 /admin（express.static + SPA fallback，不吞 /api/v1/*）；云上部署后访问 https://lujax.fun:8444/admin/ 深链/静态资源/API 均正常；桌面端 build-desktop.cjs 重打包（绿角犀-Setup-1.0.0.exe 159.4MB + 绿角犀-Portable-1.0.0.exe 158.9MB），已验证打包内无 AdminLayout/无 /admin 路由 |
| 2026-09-10 | 安卓 APK v1.0.1 正式签名 + 安装包公网上架 | mobile vite build + cap sync + gradle assembleRelease；JDK 21 + SDK 35 环境已现场补齐；根 build.gradle google() 改阿里云镜像（maven.google.com 被墙）；versionName 1.0.1 / versionCode 2，正式签名 CN=LuJiaoXi；产物 绿角犀-安卓端-v1.0.1-正式签名.apk（13.2MB，管理入口已移除）。三个安装包（Setup+Portable+APK）已上传 47.116.59.141:/usr/share/nginx/html/apk/，Nginx lujax-cloud-https.conf 新增 location /apk/ 静态托管，curl -I 206 验证通过。⚠️ lujax.fun 因未 ICP 备案在本机出口 SNI 封锁，安装包分发**务必用直连 IP 47.116.59.141** |
| 2026-09-10 | 合规落地：用户协议 + 隐私政策 + 版本 bump 1.0.1 | 新增 TermsPage.tsx（7 节用户协议）+ PrivacyPolicyPage.tsx（9 节隐私政策）纯文本页；三套路由注册（未登录放行 + 桌面端 + 移动端）；registerSchema + phoneRegisterSchema 加 agreeTerms zod refine，Prisma schema 加 agreedToTermsAt DateTime?，云端 prod.db prisma db push 成功；前端注册流程（手机+手动两条路径）加 checkbox + 前端校验兜底；版本号全落点 bump 1.0.0→1.0.1（electron/backend package.json + 前端 5 文件 8 处显示字符串）；node build-desktop.cjs 打 release-v16（绿角犀-Setup-1.0.1.exe 152MB + Portable 151.5MB）；云端部署 dist 覆盖 + pm2 restart → pm2 显示 1.0.1 online；curl 全链路验证 agreeTerms:false → 422 refine 触发，agreeTerms:true → 200 放行 ✅。**后续发现并修复打包 Bug**：首次产物启动 chrome-error://chromewebdata/ 白屏 → electron/package.json extraResources 写 `"from": "resources/frontend/dist"` 但 build-desktop.cjs 把前端直接复制到 `electron/resources/frontend/`（不带 dist 子目录）→ electron-builder 找不到 frontend 目录 → win-unpacked 里 resources/frontend 完全缺失。修复：`"from": "resources/frontend/dist"` → `"from": "resources/frontend"`。修复后重打包（`--skip-frontend --skip-backend` 只跑 electron-builder）→ Setup 160MB + Portable 160MB（多 8MB frontend bundle）。本机启动窗口标题从 `aie-desktop` 变为「绿角犀 - 你的全能个人助理」，前端正确加载。电脑控制工具 GUI 实测：登录页 v1.0.1 显示 ✅，注册页 checkbox 存在 ✅，未勾选 disabled / 勾选启用 ✅，协议链接正确 ✅。修复版 Setup + Portable 重新 SCP 覆盖服务器旧版（20:33 时间戳），HTTP HEAD 200 ✅ |

---

> 文档维护人：AI 助理（GLM-5.3）
> 最后更新：2026-09-11（v1.0.1 合规落地 + 打包 Bug 修复 + GUI 实测通过 + 公网三版本全绿）
> 项目总进度：≈ 99%
