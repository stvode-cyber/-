# 生活助理 AI - 产品需求与设计规格文档

> 整理自全对话历程，覆盖人设、交互、状态、技能、记忆、工程落地。
> 版本 v1.0 · 生效日 2026-08-20 · 以后所有设定变更都在本文档中更新。

---

## 1. 核心定位

一个以**「对话」为唯一入口**的个人生活助理 App。

- **形态**：聊天窗口（类微信），多模态输入（语音、文字、图片、文件自由混用）。
- **人设**：老朋友 / 隐形管家。**不客套、不汇报、不说教、不装专业**。直呼昵称/"你"，禁用"您/用户/欢迎回来/检测到/建议您"。
- **核心价值**：陪聊（情绪陪伴）→ 生活记录（账本、习惯、健康、事件）→ 工作轻支持（日程、文档、会议纪要），三大模块在对话流中自然流转，用户无感切换。
- **长期目标**：为每个用户静默建立一份个人知识库（Obsidian 风格），随时间积累画像、习惯、关系网，最终以「定制化周报/画像/检索」形式回馈用户，**仅在用户主动索取时显性化**。

---

## 2. 交互与状态设计

### 2.1 用户可见状态（仅 3 态）

| 状态 | 触发 | 视觉表现 |
|---|---|---|
| **IDLE** | 无进行中任务 | 头像呼吸光/静默 |
| **THINKING** | 收到输入，推理/调工具中 | 头像轻微旋转/"思考中…" |
| **RESPONDING** | 开始生成回复 | 打字机流式输出 |

**彻底移除**：LISTENING、PROACTIVE、所有模块级状态、进度条、确认卡片、主动推送、槽位追问。

### 2.2 启动问候（冷启动仅一次）

- **策略**：时间感知（早/午/晚/深夜）+ 关系深度（首见/久别/日常/连续）+ 可选碎碎念（30% 概率、高置信度）。
- **语料池**：维护在配置文件，随机抽取，像老友发微信。
- **输入框占位符**：早/午/晚「随便说说、记账、看账单…」、深夜「失眠聊聊、发呆、记灵感…」。

### 2.3 短回复处理（不接戏、不冷场、不强找话题）

| 用户输入类型 | 判断依据 | 系统动作 | 示例 |
|---|---|---|---|
| **纯确认**（好/行/嗯/ok） | 上条是陈述/完成汇报 | 不发气泡，仅触发前端 silent_ack（输入框微动+头像脉动） | — |
| **情绪停顿**（唉/烦/累/无语） | 单字情绪词 | 极短共情占位 | "在听"/"抱抱"/"想吐就吐"/"要不歇会儿？" |
| **模糊意向**（随便/都行/你看着办） | 上条有提议/选项 | 代决策→直接落地工具→回报结果 | "行，按周五中午定了，已发邀请" |
| **礼貌结束**（谢谢/晚安/拜拜） | 对话自然终点 | 温柔收尾 | "不客气，早点睡🌙"/"随时喊" |
| **噪音**（1/。/.../emoji） | 纯标点/表情 | 30% 回同款 emoji，70% silent_ack | 👍 / 静默 |

---

## 3. 技能体系（4 大内置 Skill，可插拔）

所有能力以 **Tool（Function Calling）** 形式暴露给 LLM，命名规范 `{skill}.{action}`。

| Skill | 核心能力 | 关键规则 |
|---|---|---|
| **psychology（心理陪伴）** | 情绪评估、危机识别、CBT 认知重构 | 每条消息静默跑 assess_mood+crisis_detect（silent=true）；仅高风险/极大波动才出共情占位，**不说教、不推量表、不给建议** |
| **schedule（日程助手）** | 创建/查询/冲突检测/多端同步 | 创建前必查冲突；用户说"随便/都行"→按历史偏好/最省事选项直接落地 |
| **accounting（记账本）** | 记一笔/查报表/OCR 票据/预算预警 | 缺槽位**绝不追问**，写入 pending_confirm 待确认队列，后台补全；自然语言查报表 |
| **office（办公小助手）** | 读写文档/表格、导出 PDF、转格式 | 只读不改原件，生成新版本返回预览链接；大文件分块处理 |

**扩展机制**：新 Skill 只需注册 ToolDef + 系统提示词片段，零侵入核心逻辑。

---

## 4. 静默知识库——「隐形图书管理员」

### 4.1 原则

- **理解即入库**：每轮对话后自动抽取实体/关系/偏好/事件/习惯/情绪 → 写入向量库 + 知识图谱。
- **用户零感知**：无确认、无展示、无打断。
- **用户可掌控**：设置页提供「理解开关四档（全开/仅文本/仅本地/关闭）、实体浏览器、证据溯源、一键清空/导出 Markdown+JSON」。

### 4.2 自动抽取 Schema（后台异步，火即忘）

实体、关系、偏好、事件、习惯打卡、情绪信号。

### 4.3 仅三条显性化触发指令

| 指令 | 返回形态 |
|---|---|
| `@回顾` / `@周报` | 全屏单页长图（周报卡片） |
| `@画像` | 只读画像面板 JSON |
| `@找 {query}` | 检索结果列表 |

---

## 5. 可视化周报 / 画像（用户主动唤起）

### 5.1 周报单页结构（自上而下一屏滑完）

1. **标题行**：本周小结 · 日期范围
2. **状态胶囊组**：心情均值、睡眠规律、收支平衡、运动达标、专注时长、社交热度
3. **陪聊高光**：2-3 条语录卡片
4. **财务水波/桑基图**：收入/支出/结余 + 分类占比
5. **习惯热力图**：GitHub Contribution 风格
6. **关键事件时间轴**：5-7 条
7. **老友碎碎念**：1 句（模板池+变量填充）
8. **底部操作栏**：分享长图 / 修正补充 / 调整权限

### 5.2 画像面板（只读）

作息、消费画像、情绪基线、习惯连击、关键人、待办种子。

### 5.3 碎碎念模板池（运营可配置）

如：`"记账连击 {streak} 天，比我坚持健身久多了 😄"`、`"这周 {mood} 时刻最多……"`。每周一离线渲染，前端直读缓存。

---

## 6. 隐私与控制（不可违背的红线）

1. **理解开关四档**：全开 / 仅文本 / 仅本地 / 关闭。关闭后仅聊天+调工具，不再写入向量库/图谱。
2. **数据最小化**：工具参数仅传必要字段；文件读取需用户明确发送/授权。
3. **可撤销**：每条结构化卡片可溯源到对话段落，支持单条删除/改标签/断开关系。
4. **导出权**：一键导出标准 Markdown + JSON，兼容 Obsidian/Logseq/Notion。

---

## 7. 工程落地规范（给开发团队）

### 7.1 代码组织

- **Monorepo 结构**：`packages/agent-core` 为核心包，`apps/*` 依赖它。
- **单文件入口**：`AgentCore.ts` 导出 `initAgentCore(llm, memory)` 与 `getAgentCore().handle(request)`。
- **三个适配器接口**（需方接入现有基建）：
  - `LLMClient`：统一 OpenAI/Azure/本地模型
  - `MemoryStore`：Redis/PostgreSQL/SQLite/ORM，提供用户画像、历史、KV、事件总线
  - **外部能力适配器**：日历、账本、文件、向量/图谱（在 ToolRegistry.execute 内调用）

### 7.2 运行时流程

```
用户输入 → 短回复拦截器 → 正常 LLM 流程(系统提示词+工具列表)
                                    ↓
                              首次 LLM → 工具并行执行 → 二次 LLM 总结 → 流式返回
                    ↓（并行）
            后台异步理解管线(实体/关系/偏好/事件/习惯/情绪入库)
```

### 7.3 配置热更

核心常量（人设、问候语料、周报模板、系统提示词）提取为 JSON/MD 文件，提供 `/admin/config/reload` 接口，改配置即时生效，无需重启/重新部署。

### 7.4 离线任务

- **每轮对话后**：UnderstandingPipeline 异步写库
- **每周一 03:00**：WeeklyAggregator 聚合生成周报 JSON + 长图 URL 写缓存

### 7.5 验收清单

- [ ] 短回复 5 类分支全覆盖单测
- [ ] 4 个 Skill 工具契约测试（返回 ToolResult 结构、silent=true 不进二次 LLM）
- [ ] System Prompt token < 4k，含 4 个 skill 追加片段
- [ ] 启动问候组装逻辑输出符合老友语气
- [ ] 隐私面板四档开关、实体浏览器、证据溯源、一键清空/导出全联调通过

---

## 8. 迭代优先级建议

| 优先级 | 范围 | 交付物 |
|---|---|---|
| **P0** | 核心框架 + 记账 | AgentCore 单例、accounting 全链路、短回复拦截、静默理解管线、启动问候 |
| **P1** | 日程 + 周报 | schedule 三件套、周报单页前端、周一离线聚合 Job |
| **P2** | 心理 + 办公 | psychology 情绪/危机/重构、office 读写导出 |
| **P3** | 画像 + 检索 | @画像 面板、@找 检索、隐私控制面板 |
| **P4** | 运营配置化 | 碎碎念模板后台管理、热更接口、A/B 测试语料池 |

---

## 9. 一句话给全员

把「老朋友」人设刻进 System Prompt，把四大能力做成 Tool，把「理解即入库」做成后台管线，把「仅用户主动取用」做成唯一显性化入口。所有 HTTP 入口只调 `agentCore.handle(request)`，改设定只改配置文件，加能力只注册 Tool——这才是能长期存活、随业务演进的架构。
